# Dependências

```bash
pkg update -y
pkg upgrade -y

pkg install nodejs -y
pkg install chromium -y
```

```bash
npm init -y
```

```bash
npm install puppeteer-core express cors body-parser axios
```
